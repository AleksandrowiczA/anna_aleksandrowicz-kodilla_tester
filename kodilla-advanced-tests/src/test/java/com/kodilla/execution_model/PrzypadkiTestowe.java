package com.kodilla.execution_model;

public class PrzypadkiTestowe {

    // 1. Test dodawania nowego zamówienia:
    // sprawdzenie, czy po dodaniu zamówienia ilość zamówień w sklepie zwiększa się o 1,
    // sprawdzenie, czy dodane zamówienie znajduje się w kolekcji zamówień sklepu.

    // 2. Test zwracania listy zamówień z zakresu dwóch dat:
    // sprawdzenie, czy lista zamówień zwracana przez metodę zawiera tylko zamówienia z zakresu dat, które przekazaliśmy jako parametry,
    // sprawdzenie, czy ilość zamówień w zwróconej liście zgadza się z oczekiwaną ilością.

    // 3. Test pobierania zamówień na podstawie przekazanego zakresu wartości zamówienia:
    // sprawdzenie, czy zamówienia zwracane przez metodę znajdują się w zakresie wartości, który przekazaliśmy jako parametry,
    // sprawdzenie, czy ilość zamówień zwróconych przez metodę zgadza się z oczekiwaną ilością.

    // 4. Test zwracania liczby zamówień:
    // sprawdzenie, czy ilość zamówień zwracana przez metodę zgadza się z ilością zamówień przechowywanych w kolekcji zamówień.

    // 5. Test sumowania wartości wszystkich zamówień:
    // sprawdzenie, czy wartość zwracana przez metodę zgadza się z sumą wartości wszystkich zamówień przechowywanych w kolekcji zamówień.
}
